---
description: Add a shadcn/ui component
---
1. Check if component exists in `apps/web/components/ui/`
2. If missing, run `cd apps/web && bunx --bun shadcn@latest add <component-name>`
// turbo
3. Import from `@/components/ui/<component-name>`
