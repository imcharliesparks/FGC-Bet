You are working inside the React Native app at `apps/mobile`.

Rules:
- Expo-based project
- No Node.js APIs
- Shared logic must live in `packages/`
- Use React Query for async state
- Use platform-safe components only
