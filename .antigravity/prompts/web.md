You are working inside the Next.js app located at `apps/web`.

Rules:
- Use App Router (`app/`)
- Use Server Components by default
- Client components must include `"use client"`
- Use shadcn/ui and Tailwind for UI
- Do not import React Native code

If unsure, ask before creating new files.
